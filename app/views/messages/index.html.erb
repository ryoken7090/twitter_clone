<div class="contents">
  <% @messages.each do |message| %>
      <p><%= link_to message.content, message_path(message.id),{class: "content"} %></p>
  <% end %>
</div>
<%= link_to '***** つぶやく *****', new_message_path %>
